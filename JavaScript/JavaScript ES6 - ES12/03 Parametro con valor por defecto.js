
/* Son valores que ya vienen predefinidos en la funcion, no hace falta que sean 
parametros de entrada, ejemplo: */

let getPrecioFinal = (precio, impuesto = 0.7) => precio + precio * impuesto;

getPrecioFinal(500); // 850



